# Prims-Algorithm
Prim's Algorithm generates a minimum spanning tree by starting from one node and repeatedly adding the lowest-cost edge that connects a visited node to an unvisited node until all nodes are connected. It creates a connected graph with no cycles.
